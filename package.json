{
  "name": "architectural-portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/generate-manifest.js && vite build",
    "preview": "vite preview",
    "refresh-projects": "node scripts/generate-manifest.js"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}